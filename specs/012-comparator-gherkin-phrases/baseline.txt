# Baseline evidence for 012-comparator-gherkin-phrases (T002)

Captured 2026-09-10 on branch `012-comparator-gherkin-phrases`, created from `main` at `0f9dcea`
with the Tempo harness up (`make harness-up`, 8 containers).

This is the **before** half of SC-009 (zero cost when unused) and SC-012 (`Strict` churns no
golden). The **after** half is recorded by T006/T007 and re-confirmed by T066.

---

## 1. `go test ./...` — all non-e2e packages

    all ok, 0 FAIL

## 2. `go test -tags e2e -timeout 25m ./e2e/` — live Tempo

    ok  github.com/thetonymaster/mentat/e2e  40.101s

`make ci` does NOT compile this lane. A green `make ci` is not evidence for SC-012.

## 3. Per-package coverage (`go test ./... -coverprofile=... && go tool cover -func=...`)

| Package | Baseline coverage |
|---|---|
| `github.com/thetonymaster/mentat` (facade) | 99.1% |
| `cmd/mentat` | **69.4%** |
| `cmd/mentatctl` | **31.4%** |
| `e2e` | 100.0% |
| `internal/cel` | 87.3% |
| `internal/comparator` | 95.0% |
| `internal/config` | 100.0% |
| `internal/core` | 100.0% |
| `internal/correlate` | 98.8% |
| `internal/ctl` | 91.0% |
| `internal/driver` | 94.5% |
| `internal/engine` | 95.2% |
| `internal/expectations` | 91.6% |
| `internal/judge` | 97.8% |
| `internal/registry` | 90.3% |
| `internal/report` | 95.1% |
| `internal/result` | 100.0% |
| `internal/steps` | 88.9% |
| `internal/store` | 95.4% |
| `internal/testio` | 92.9% |
| `internal/trace` | 94.4% |
| `tracelab/orderflow` | 93.4% |
| `tracelab/orderflow/cmd/orderflow` | 48.6% |
| `tracelab/researchbot` | 92.9% |

### Note on the 80% floor (affects T063)

`cmd/mentat` (69.4%) and `cmd/mentatctl` (31.4%) are **already below** the constitution's 80%
per-package floor at `0f9dcea`. 012 touches `cmd/mentat`.

T063's obligation is therefore read as: packages at or above 80% must stay at or above 80%;
packages already below must **not regress**. This is recorded rather than silently assumed,
because reinterpreting a gate without saying so is how gates quietly stop meaning anything.

The packages 012 actually changes and their floors to hold:

| Package | Baseline | Obligation |
|---|---|---|
| `internal/core` | 100.0% | stay ≥ 80% (expect ~100%) |
| `internal/steps` | 88.9% | stay ≥ 80% |
| `internal/engine` | 95.2% | stay ≥ 80% |
| `github.com/thetonymaster/mentat` (facade) | 99.1% | stay ≥ 80% |
| `cmd/mentat` | 69.4% | no regression below 69.4% |

---

## 3a. SC-009's "byte-identical" needs one caveat (found 2026-09-11)

SC-009 says an engine with zero contributed phrases must produce **byte-identical** output to
`0f9dcea` across every existing golden. Taken literally that is unachievable, and not for any
behavioural reason.

godog's pretty formatter annotates each step with the **source location** of the step definition
it bound:

    Given the agent target "bot"        # metadata.go:75 -> *world

`testdata/golden-hermetic.txt` captured that verbatim, so adding a comment anywhere above the
`reg.Step` loop in `metadata.go` churns the golden — `metadata.go:75` became `metadata.go:107`
with the rendered output otherwise identical.

**Resolution**: `normalizeGoldenStdout` now rewrites the line number to `<LINE>`, keeping the
filename. Registration moving to a different file still churns the golden (a real change to the
single registration path); the line within that file does not.

SC-009 should be read as **byte-identical after the golden's own normalization** — which is
already how it treats the trailing duration line (`<DURATION>`). This is a clarification of an
existing convention rather than a new exemption.

---

## 3b. Final coverage (T064) — re-measured, and why `cmd/mentat` fell

An earlier version of this section recorded numbers that **did not reproduce**: review
re-ran them and found root at 94.3% where 95.1% was written, and `internal/steps`
annotated "above baseline" when it was equal. That is the same failure this feature kept
finding in itself — a figure written down and never re-run. The numbers below were
measured after the final commit, with `main`'s taken from its own worktree at `0f9dcea`
rather than remembered.

| Package | `main` @ `0f9dcea` | branch (final) | Verdict |
|---|---|---|---|
| `github.com/thetonymaster/mentat` | 99.1% | **96.1%** | ≥ 80% ✓ (below baseline; new error paths on two entry points) |
| `internal/core` | 100.0% | 100.0% | ≥ 80% ✓ |
| `internal/engine` | 95.2% | 95.0% | ≥ 80% ✓ |
| `internal/steps` | 88.9% | **89.3%** | ≥ 80% ✓, above baseline |
| `cmd/mentat` | 69.4% | **62.0%** | below baseline — see below |

Commands: `go test -count=1 ./... -coverprofile=…` on this branch; the same in
`/private/tmp/mentat-main` for `main`. `-count=1` matters — a cached run reports the
previous code's numbers, which is how the stale figures survived.

### Why `cmd/mentat` fell, measured rather than asserted

| | statements | covered | % |
|---|---|---|---|
| `cmd/mentat` baseline | 281 | 195 | 69.4% |
| `cmd/mentat` after | 187 | 116 | 62.0% |
| `internal/steps` baseline | 442 | 393 | 88.9% |
| `internal/steps` after (at the point this was computed) | 687 | 614 | 89.4% |
| **combined baseline** | 723 | 588 | **81.3%** |
| **combined after** | 874 | 730 | **83.5%** |

The feature-walking logic (`checkFeature`, `featureFiles`, `lineMap`, `newIDGen`,
`dedupeSort`) MOVED from `cmd/mentat` into `internal/steps` so the CLI and the library
validate entry point share one implementation. That removed 94 statements from
`cmd/mentat`, **79 of them covered** — an 84%-covered block, well above the package's
69.4% average. Deleting an above-average block lowers the average; no coverage was lost,
and the combined figure rose.

What remains uncovered in `cmd/mentat` is `main`, `usage` and `runMain` — 0% at baseline
too, being the `os.Exit` shell the pure seams exist to keep untested.

**Why `make ci` is green regardless**: the coverage gate
(`.claude/skills/coverage/coverage.sh:44-47`) SKIPS `*/cmd/*`, `*/cmd`, `*/mocks` and
`*/mocks/*`. `cmd/mentat` has therefore never been subject to the 80% floor and its
number is informational. Stated because the argument above would otherwise look like
what keeps CI green, and it is not.

**Conclusion**: two packages sit below their baselines. `cmd/mentat`'s is arithmetic and
explained above. The facade's 99.1% → 96.1% is real: `Validate` and `StepReference` added
error paths, and the ones left uncovered are seam-construction failures already covered
through `Run`. Both remain far above the floor.

---

## 4. Environment

- Go 1.25.0
- `github.com/cucumber/godog v0.15.1` (pinned — R0 and R1 are properties of this version)
- `github.com/cucumber/messages/go/v21 v21.0.1`
- Harness: `deploy-tempo-1`, `deploy-collector-1`, 6 orderflow SUTs, all up

---

## 5. After — SC-012 re-measured with `Strict: true` (T006/T007)

Measured 2026-09-11, immediately after T005 added `Strict: true` to `run.go`.

### T006 — `go test ./...` with `Strict: true`

    all ok, FAIL count = 0     (identical to §1)

Includes the hermetic stdout golden `mentat_golden_test.go`, which did not change.

### T007 — `go test -tags e2e -timeout 25m ./e2e/` with `Strict: true`

    ok  github.com/thetonymaster/mentat/e2e  39.554s     (baseline: 40.101s)

Includes the SC-005 stdout golden, which did not change. Wall-clock varies run to run; what
SC-012 asserts is that **golden content** does not, and no golden test failed.

### Verdict

**Zero churn on both surfaces.** R0's planning-time measurement reproduced at implementation
time rather than inherited — which matters, because R1 (the other planning-time claim about
godog) turned out to be partly false when re-checked (see R10).

---

## 6. Final verification (T066/T067), and the golden `make ci` could not see

Run 2026-09-11 with the harness up, after all implementation.

| Surface | Result |
|---|---|
| `gofmt -l .` | clean |
| `go vet ./...` | clean |
| `golangci-lint run ./...` | **0 issues** |
| `go test ./...` | all ok, 0 FAIL |
| `go test -tags e2e -run TestGolden ./e2e/` | `ok 15.866s` |
| `go test -tags e2e -timeout 25m ./e2e/` | `ok 35.660s` (baseline 40.101s) |
| `make ci` | PASS, including `COVERAGE GATE: PASS (all packages >= 80%)` |

### The e2e golden caught what `make ci` structurally cannot

The SC-005 stdout golden (`cmd/mentat/testdata/golden-green.txt`) captured the same
step-definition source annotation §3a describes — `# metadata.go:75 -> *world` — and 012's
documentation of `registerSteps` moved that line to 107. The e2e lane FAILED on it.

`make ci` does not compile `//go:build e2e`, so nothing caught this until the e2e run. That is
exactly why the spec refused to let a green `make ci` discharge SC-012, and it is the second time
in this feature that running both surfaces was the only thing that worked.

**Fix**: `normalizeStdout` in `e2e/golden_test.go` now applies the same line-number normalization
as `normalizeGoldenStdout` in the root package, and the committed golden was regenerated
(7 insertions, 7 deletions — every one of them a `metadata.go:NNN` → `metadata.go:<LINE>` token,
no rendered content changed). The two goldens capture the same annotation and now treat it the
same way; a comment on either side would otherwise churn one and not the other.

**Standing lesson**: a green `make ci` is not evidence about the e2e goldens. Run
`go test -tags e2e` after any change to step registration, and after any change to the
FILE LAYOUT around it — not only to the step table.

# Go
/bin/
*.out
cover.out
/researchbot
/capture
/mentat
/mentatctl

# Python (skill scripts)
__pycache__/
*.pyc

# Local
.env

# SpecKit — tooling (skills, scripts, templates, extensions, workflows, integration
# manifests) is TRACKED so it is present in every git worktree, not just the original
# checkout. Only transient per-feature working state stays ignored.
/.specify/feature.json
